function Auth() {
  return (
    <div style={{ marginTop: "5rem" }}>
      <h3>Auth Page Content</h3>
      <div
        style={{
          display: "flex",
          justifyContent: "center",
          marginBottom: "20px",
        }}
      >
        <i>(Auth Page with only Footer)</i>
      </div>
    </div>
  );
}

export default Auth;
